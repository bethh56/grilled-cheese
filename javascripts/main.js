// X div in html - id="bread-container" 
// X printToDom function  in utils 
// X breadData will have array of bread objects, export a getBread function
// X each bread object = type, color, price, id
// X breads.js componenet - with domstring builder (find something nice in bootstrap)
// X import breads component into main
// event listner on radio button - call breadData.setSelectedBread(breadId)
// setSelectedBread = assign a variable (selectedBreadId) to the id passed in
import breads from './components/breads.js';

const init = () => {
    //breads.makeBreadRadioButtons();
};

init();